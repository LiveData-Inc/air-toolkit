# {{ name }}

**Created:** {{ created }}
**Mode:** {{ mode }}

## Overview

This is an AIR (AI Review) assessment project for analyzing and comparing multiple codebases.

## Project Mode: {{ mode }}

{% if mode == "review" -%}
This is a **review-only** project. All linked repositories are for analysis purposes only.

**Workflow:**
1. Review linked resources in `review/`
2. Create analysis documents in `analysis/assessments/`
3. Generate summary in `analysis/SUMMARY.md`

{% elif mode == "collaborate" -%}
This is a **collaborative** project. You can contribute improvements to linked repositories.

**Workflow:**
1. Review linked resources in `collaborate/`
2. Identify improvement opportunities
3. Create contributions in `contributions/`
4. Submit pull requests via `air pr`

{% else -%}
This is a **mixed-mode** project supporting both review and collaborative workflows.

**Review Workflow:**
1. Review resources in `review/`
2. Create analysis in `analysis/assessments/`

**Collaborative Workflow:**
1. Review resources in `collaborate/`
2. Create contributions in `contributions/`
3. Submit pull requests via `air pr`

{% endif -%}

## Getting Started

### Link Repositories

```bash
{% if mode == "review" or mode == "mixed" -%}
# Link review-only resources
air link --review service-a:~/repos/service-a
air link --review service-b:~/repos/service-b

{% endif -%}
{% if mode == "collaborate" or mode == "mixed" -%}
# Link collaborative resources
air link --collaborate docs:~/repos/documentation

{% endif -%}
```

### Validate Structure

```bash
air validate
```

### Check Status

```bash
air status
```

## Project Structure

```
{{ name }}/
├── README.md              # This file
├── CLAUDE.md              # AI assistant instructions
├── air-config.json        # Project configuration
{% if mode == "review" or mode == "mixed" -%}
├── review/                # Review-only resources (symlinks)
{% endif -%}
{% if mode == "collaborate" or mode == "mixed" -%}
├── collaborate/           # Collaborative resources (symlinks/clones)
{% endif -%}
├── analysis/              # Your analysis
│   ├── SUMMARY.md         # Executive summary
{% if mode == "review" or mode == "mixed" -%}
│   ├── assessments/       # Analysis of review resources
{% endif -%}
{% if mode == "collaborate" or mode == "mixed" -%}
│   └── improvements/      # Feedback on collaborative resources
{% endif -%}
{% if mode == "collaborate" or mode == "mixed" -%}
├── contributions/         # Proposed improvements
{% endif -%}
├── .air/                   # AI task tracking
│   ├── tasks/            # Task files
│   └── context/          # Architecture, conventions
└── scripts/              # Utility scripts
```

## Commands

```bash
# Project management
air init <name>            # Create new project
air link                   # Link repositories
air validate               # Validate structure
air status                 # Show project status

# Task tracking
air task new <desc>        # Create task file
air task list              # List tasks
air summary                # Generate summary

{% if mode == "collaborate" or mode == "mixed" -%}
# Contributions
air pr <resource>          # Create pull request

{% endif -%}
# Utilities
air version                # Show version
```

## Goals

<!-- Add your assessment goals here -->

{% if goals -%}
{% for goal in goals -%}
- {{ goal }}
{% endfor -%}
{% else -%}
- [ ] Review architecture and design patterns
- [ ] Identify gaps and improvement opportunities
- [ ] Create comprehensive analysis
{% if mode == "collaborate" or mode == "mixed" -%}
- [ ] Contribute improvements back to repositories
{% endif -%}
{% endif -%}

## Resources

<!-- Linked repositories will be listed here -->

## Notes

<!-- Add project-specific notes here -->
